`git init` initializes folder for gits reference for backup
create readme.md
`git config user.name 'name'` {establishing username & email before commit}
`git config user.email 'email'`
committed using ui
`git branch -M main` renamed branch
